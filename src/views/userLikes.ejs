<!DOCTYPE html>
<html lang="en">

        <%- include ("partials/head.ejs") %>
        <!-- <link rel="stylesheet" href="/css/userLikes.css" /> -->
        <link rel="stylesheet" href="/css/userLikes.css">
    </head>

    
    <%- include ("partials/header") %>
    <body class="background">
        <main>

            <div class="below-head-bar">
                
                <% if (user.products[0]) { %> 
                <% for (let product of user.products) { %> 
                    <li>
                        <div class= "product-image"> 
                             <img src="/../images/productos/<%= product.image %>" alt="imagen" width="100%" />                    
                         </div>
                    <%= product.name %> ---- <a href="/users/profile/likes/delete/<%= product.id %> "><button>borrar</button></a>
                    
                    </li>
                    <% } %> 
                    <% } else { %>
        
                        No tenes likes
        
                        <% } %> 
        
                    <a href="/users/profile">ir a tu perfil</a>

            </div>
        </main>
    </body>

</html>